run:
  # Path to the training run directory (relative to human-feedback-rl/)
  dir: "experiments/christiano/2026-03-18/10-13-21"

agent:
  # Path to the policy checkpoint (relative to human-feedback-rl/)
  model: "experiments/christiano/2026-03-18/10-13-21/models/policy_christiano"

eval:
  episodes: 100

hydra:
  run:
    dir: /tmp/hydra_eval/${now:%Y-%m-%d_%H-%M-%S}
  output_subdir: null
