[project]
name = "human-feedback-rl"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "torch>=2.0.0",
    "tensorboard>=2.13.0",
    "hydra-core>=1.3.0",
    "omegaconf>=2.3.0",
    "tqdm>=4.65.0",
    "matplotlib>=3.7.0",
    "human-preferences-rl @ git+https://github.com/andrea02polimi/learning-from-human-preferences.git@master",
]

[tool.setuptools.packages.find]
where = ["."]